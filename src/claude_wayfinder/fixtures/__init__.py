"""Package marker for bundled demo fixtures.

This package ships two JSON files used by ``python -m claude_wayfinder demo``
and the ``skills/dispatch/SKILL.md`` plugin skill:

- ``demo-catalog.json`` — a minimal dispatch catalog with entries chosen to
  exercise all 7 decision branches.
- ``demo-prompts.json`` — 7 example dispatch contexts, one per branch.
"""
